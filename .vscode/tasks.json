{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Build using Makefile",
			"command": "make",
			// "options": {
			// 	"cwd": "${fileDirname}"
			// },
			"problemMatcher": ["$gcc"],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"detail": "compiler: /usr/bin/clang++"
		}
	]
}